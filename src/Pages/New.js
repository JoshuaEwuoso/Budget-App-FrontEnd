import React from "react";
import TransactionNew from "../Components/TransactionNew";

const New = () => {
    return (
        <div>
            <h2>New Transaction</h2>
            <TransactionNew />
        </div>
    );
}

export default New;